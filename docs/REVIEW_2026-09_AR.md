# مراجعة شاملة لمنصة HELM Portal — وتقرير التطوير والتعديل

**التاريخ:** 11 سبتمبر 2026
**النطاق:** مستودع `ahmedhelmy200-rgb/HELM-PORTAL` بالكامل (الكود + قاعدة البيانات + النشر + سطح المكتب)
**الفرع:** `arena/01a08e85-helm-portal` — منشعب من `main@8d28b39`

---

## 1. الملخص التنفيذي

المنصة **تعمل وتبني بنجاح**، وتحتوي على قدر كبير من العمل الجاد: بوابة موكّلين مع RLS، نشاط سمعي/بصري، 88 صفحة SEO عربية وإنجليزية، فاتورة إلكترونية بـ Stripe، ونشر على Vercel + Cloudflare + تطبيق Windows.

لكن المراجعة كشفت **ثلاثة عيوب تمسّ المال والخصوصية والأمان**، إضافة إلى **غياب أي شبكة أمان هندسية** (لا اختبارات، لا بوابة جودة). التوصية الأساسية ليست إعادة كتابة، بل **تثبيت الأساس**: تصحيح الأخطاء الموجودة، ثم إغلاق ثغرات الصلاحيات في قاعدة البيانات، ثم تحسين الأداء.

### بطاقة الصحة قبل/بعد هذه المراجعة

| المؤشر | قبل | بعد | الحالة |
|---|---|---|---|
| بناء الإنتاج (`npm run build`) | ✅ ناجح | ✅ ناجح | سليم |
| أخطاء ESLint | ❌ **35 خطأ** | ✅ **0** | مُصلح |
| اختبارات آلية | ❌ **صفر** | ✅ **81 اختبارًا** | مُضاف |
| بوابة جودة في CI | ❌ لا يوجد | ✅ `ci.yml` | مُضاف |
| رؤوس أمان في الإنتاج | ⚠️ 4 رؤوس | ✅ 8 رؤوس + CSP | مُحسّن |
| ثغرة XSS في نوافذ الطباعة | ❌ 3 مواضع | ✅ مُعالجة | مُصلح |
| رابط دفع الفواتير | ❌ **يفشل لمعظم المعرّفات** | ✅ يعمل | مُصلح |
| حساب الفواتير بقيم تالفة | ❌ يُنتج `NaN` | ✅ يُعامل كصفر | مُصلح |
| ملفات مفتاح مُموّهة ميتة | ❌ 4 ملفات | ✅ محذوفة | مُصلح |

---

## 2. منهجية المراجعة

لم يُعتمد الحكم على قراءة الكود فقط، بل على تنفيذ فعلي:

```bash
npm ci                      # 652 حزمة
npm run build               # نجح — 9.3 ثانية، 88 أصلاً
npm run lint                # كان يعيد exit code = 1 (فشل)
npm run typecheck           # يفشل (أخطاء سابقة للمراجعة)
npm test                    # لم يكن موجودًا
```

بالإضافة إلى: تحليل ثابت لكل ملفات `src/` (221 ملف، 20,460 سطر JS/JSX)، و31 ملف SQL، و89 إعادة كتابة في `vercel.json`، وإعدادات Electron، ومقارنة ملفات SEO مع خريطة الموقع.

---

## 3. ما تم تنفيذه فعليًا في هذه المراجعة

### 3.1 إصلاح ثغرة XSS مخزّنة تصل إلى جلسة الدخول 🔴

**المشكلة:** ثلاثة مواضع تبني HTML نصيًا وتكتبه في نافذة منبثقة **من نفس الأصل** عبر `window.open("")` + `document.write`، مع إدراج قيم مصدرها قاعدة البيانات دون تهريب:

| الملف | القيمة المُدرجة بلا تهريب |
|---|---|
| `src/components/helm/GenerateDocumentDialog.jsx` | `${generatedContent}` — قالب معبّأ باسم الموكّل وجنسيته وعنوانه ووصف القضية |
| `src/pages/LegalTemplates.jsx` | `${t.title}` و `${t.content}` |
| `src/pages/Invoices.jsx` | `${previewInvoice?.invoice_number}` |

**سلسلة الاستغلال:** موكّل يسجّل عبر بوابة التسجيل ويضع اسمه الكامل = `<img src=x onerror="...">` → المحامي يولّد وثيقة من القالب → الحمولة تُنفَّذ في نافذة **من نفس الأصل**، وواصلها `window.opener` يمنحها وصولًا كاملًا للبوابة وقراءة رمز الجلسة من `localStorage` → **استيلاء كامل على حساب الموظف**.

**المعالجة:** أُضيف `src/lib/htmlEscape.js` (مع `escapeHtml` و `detachOpener`)، وطُبّق على المواضع الثلاثة، مع قطع رجوع النافذة إلى البوابة (`win.opener = null`) وإظهار تنبيه عند حجب النوافذ المنبثقة بدل الانهيار.

**التحقق:** 10 اختبارات في `src/lib/htmlEscape.test.js` تشمل الحمولة الفعلية أعلاه.

### 3.2 إصلاح رابط الدفع — عيب يؤثر على التحصيل 🔴

**المشكلة:** `parsePaymentToken` في `src/lib/paymentLinks.js` كان يضيف حشو base64 ثابتًا `'=='` بدل حسابه من طول الرمز. النتيجة المقيسة:

| معرّف الفاتورة | طول الرمز ÷ 4 | النتيجة قبل الإصلاح |
|---|---|---|
| `INV-2026-001` | 3 | ❌ `Invalid character` |
| `inv-9` | 0 | ❌ `Invalid character` |
| `inv-1234` | 0 | ❌ `Invalid character` |
| UUID (36 حرفًا) | 2 | ✅ يعمل — **بالصدفة فقط** |

أي رابط دفع معرّفه ليس UUID صار يظهر للموكّل كـ **«رابط الدفع غير صالح أو منتهي»** ولا يمكنه الدفع. كما أن الرمز يعمل اليوم للـ UUIDs بسبب صدفة رياضية في طول الطابع الزمني (13 رقمًا)، وينكسر تلقائيًا عند أي تغيير.

**المعالجة:** حساب الحشو الصحيح من الطول الفعلي، مع رفض الأطوال غير القابلة للإكمال، والتحقق من أن الناتج كائن JSON.

**التحقق:** 13 اختبار انحدار تلقائي على 9 أشكال مختلفة من المعرّفات — **بدون أي تراجع** للروابط العاملة حاليًا (الـ UUID يعطي نفس الحشو `==`).

### 3.3 إصلاح ظهور `NaN` في المبالغ 🟠

`getInvoiceTotals` في `src/lib/invoiceMath.js` كان يستخدم `Number(x || 0)`، وهو ما لا يحمي من النص غير الرقمي. فاتورة واحدة بقيمة `total_fees = "غير محدد"` (شائعة بعد الاستيراد من Excel) تنشر `NaN` في كل بطاقات اللوحة والتقارير. أُضيف `toAmount()` كتحويل آمن ومُصدَّر للاستخدام في بقية الشاشات.

كما أُصلح انهيار `clientIdentity(null)` في `src/lib/dataIntegrity.js` (كان المعامل الافتراضي `= {}` لا يشمل `null`).

### 3.4 حذف ملفات مفتاح مُموّهة وميتة 🔴

وُجدت أربعة ملفات بلا أي استخدام في المستودع (لا استيراد واحد):

```
src/lib/xortest.js    →  const x = 'kfCQy5HmaZHwkMyQzWmQzJHhdeaR75DD'
src/lib/chunks.js     →  export const pieces = ['2L','nZ','gt',...]   (16 قطعة × حرفان)
src/lib/chartest.js   →  نص عربي مُشفَّر بأرقام
src/lib/utest2.js     →  نص عربي كنقاط Unicode
```

النمط (تقسيم سلسلة إلى قطع + سلسلة base64-الشكل بطول 32) هو **أسلوب تجميع مفتاح داخل حزمة المتصفح**. الملفات حُذفت لأنها ميتة بلا نزاع، لكن الأهم إجرائيًا: **إذا كان أي مفتاح حقيقي قد مرّ من هذا المسار يومًا، فيجب تدويره فورًا** (Gemini / Stripe / أي خدمة). الحذف لا يكفي لأن الملفات محفوظة في تاريخ Git.

### 3.5 رؤوس أمان الإنتاج (`vercel.json`)

أُضيفت: `Strict-Transport-Security` (سنتان + preload)، `Cross-Origin-Opener-Policy: same-origin-allow-popups` (متوافقة مع نوافذ Google OAuth)، `X-DNS-Prefetch-Control`.

وأُضيفت **CSP بوضع التقرير فقط** (`Content-Security-Policy-Report-Only`) تغطي فعليًا كل أصل خارجي مستخدم: Supabase (https + wss)، Stripe، Google APIs/الشيفرة، وإطار HELM Smart على `helm-smart.pages.dev`. الوضع «تقرير فقط» لا يكسر شيئًا، ويجمع الإنذارات في وحدة تحكم المتصفح قبل التحويل إلى **إلزامي**.

### 3.6 شبكة أمان هندسية (الأهم على المدى الطويل)

- **Vitest** أُضيف مع `npm test` و `npm run verify` (فحص + اختبارات + بناء).
- **81 اختبارًا** في 5 ملفات تغطي منطق الأعمال الحسّاس: حسابات الفواتير والضريبة، توحيد أرقام الهواتف الإماراتية، كشف تكرار الموكلين والفواتير، البحث العربي المُطبَّع (التشكيل والألف والهمزة)، رموز الدفع، ونتائج القضايا.
- **`.github/workflows/ci.yml`** — بوابة جودة على كل PR ودفع إلى `main` و `arena/**`: فحص + اختبارات + بناء + التحقق من صحة `vercel.json`.
- **إصلاح 35 خطأ ESLint** في 15 ملفًا (كلها استيرادات غير مستخدمة) — الآن `npm run lint` نظيف، وهو شرط لعمل البوابة.
- `.nvmrc` (Node 20) و `engines` في `package.json`، وتوحيد الإصدار إلى `1.0.0` ليتوافق مع `electron-builder.yml` (كان `0.0.0` فينتج مُثبِّتًا باسم `HELM-Portal-Setup-0.0.0-x64.exe`).
- `.gitignore`: استثناء `release/` ومضغوطات الباتشات، وإخراج `HELM_SMART_NEW_PATCH_FOR_PORTAL.zip` (108 KB ثنائي) من التتبع.

> **ملاحظة:** تعذّر عليّ تشغيل التطبيق فعليًا مع قاعدة Supabase حقيقية، لذا كل تعديل واجهة تحقّق بالبناء والفحص والاختبارات لا بالنقر اليدوي. ينبغي اختبار مسارَي «طباعة وثيقة» و«رابط دفع» يدويًا قبل النشر للإنتاج.

---

## 4. النتائج المُكتشفة — حسب الخطورة

### 🔴 حرجة

| # | النتيجة | الموضع | الأثر |
|---|---|---|---|
| C1 | **مفتاح Gemini مسموح في قالب البيئة**: `VITE_GEMINI_API_KEY` | `.env.example` | كل متغيّر يبدأ بـ `VITE_` يُدمج نصًا داخل حزمة المتصفح. أي مفتاح يوضع هناك يصبح **عامًا لأي زائر** خلال ثوانٍ. المفتاح لا يُقرأ حاليًا في كود البوابة (مُشتت)، لكن وجوده في القالب دعوة لتنفيذه بطريقة خاطئة |
| C2 | **كود HELM Smart يحمل مفتاحًا في المتصفح**: `new GoogleGenAI({ apiKey: process.env.API_KEY })` | `src/smart/services/geminiService.ts:50,74,121,192` | `process` غير معرّف أصلًا في المتصفح (سيرمي `ReferenceError`)، وفي حال «إصلاحه» بـ `import.meta.env` سيصبح المفتاح عامًا. متوقّف حاليًا لأن الملف ضمن كود ميت |
| C3 | **قواعد الصلاحيات مكتوبة داخل حزمة المتصفح** كبريد شخصي مُثبَّت | `src/App.jsx:30`، `src/components/shared/ActionButtons.jsx:8` | `mahmoudmegally3@gmail.com` مكتوب صراحةً. هذا (أ) كشف بيانات شخصية في ملف عام، (ب) منع الحذف مفروض في الواجهة فقط — أي طلب مباشر إلى Supabase يتجاوزه. **صلاحية «غير مخوّل بالحذف» يجب أن تكون سياسة RLS لا شرطًا في JavaScript** |
| C4 | **ملفات المفتاح المُموّهة** | `src/lib/{xortest,chunks,chartest,utest2}.js` | حُذفت — راجع 3.4 لخطوة تدوير المفاتيح الواجبة |

### 🟠 عالية

| # | النتيجة | الموضع |
|---|---|---|
| H1 | **`base44Client.js` كطبقة أمان واحدة**: 271 سطرًا تُنفّذ كل شيء (مصادقة، CRUD، تحديد نطاق العميل والبروكر، تخزين، OCR، تخزين مؤقت، realtime) بأسطر مضغوطة يصعب تدقيقها. أي ثغرة في `sanitizeWritePayload` أو `applyBrokerScope` تفتح كل الجداول | `src/api/base44Client.js` |
| H2 | **تصفية الحقول الحسّاسة في الواجهة** (`stripStableClientFields` يمنع كتابة `client_id`/`user_id`) — حماية في المتصفح لا في القاعدة. يجب أن تفرض RLS الـ `client_id` الصحيح عبر `auth.uid()` | `src/api/base44Client.js` |
| H3 | **انحراف ملفات الهجرة**: 22 ملف SQL فيها **أرقام مكررة** (`003`، `010`، `022`) و**15 رقمًا مفقودًا** (`004`، `005`، `013`–`021`، `028`)، مع 3 ملفات SQL أخرى في جذر المشروع (`UNIFIED_SUPABASE_SETUP.sql` 25 KB، `FINAL_SECURITY_HARDENING.sql`، `EMAIL_PASSWORD_AUTH_SETUP.sql`) | `supabase/migrations/` + الجذر |
| H4 | **خطر ترتيب تنفيذ SQL**: دليل التشغيل يطلب تنفيذ ملفات الجذر «بعد» ملفات أخرى يدويًا. تشغيلها بترتيب مختلف قد يُعيد سياسات RLS قديمة ويُضعف الحماية بصمت | README |
| H5 | **صفر تغطية اختبارية** على منطق مالي وقانوني — أُضيفت 81 اختبارًا لكن مساحة المخاطر أوسع بكثير | كل `src/pages` |
| H6 | **`src/smart/` = 10,981 سطرًا من كود ميت**: 44 ملفًا لا يُستخدم منها إلا `ImportantLinks.tsx`. تطبيق ثانٍ كامل (TypeScript) داخل مشروع JavaScript، ويُشوّش أي تدقيق أمني مستقبلي — C2 اختبأ داخله | `src/smart/` |

### 🟡 متوسطة

| # | النتيجة | الرقم/الموضع |
|---|---|---|
| M1 | **`recharts` على المسار الحرج**: تُحمَّل المكتبة كاملة (431 KB / **114.7 KB مضغوط**) لرسم **مخطط شرارة صغير** في اللوحة الرئيسية، وهي أول صفحة يراها كل مستخدم | `src/pages/DashboardOptimized.jsx:4` |
| M2 | **9 عائلات خطوط عربية** تُحمَّل في أول تحميل عبر `@import` داخل CSS (وهي أسوأ حالة: تحجب تحليل CSS نفسه). العائلات **ميزة مقصودة** (اختيار الخط في الإعدادات)، لكن يجب تحميل خط المستخدم المختار فقط عند الطلب | `src/index.css:1` |
| M3 | **حزمة الدخول 413 KB** (125.5 KB مضغوط) + `supabase` 217 KB (54.3 KB). لا تقسيم لـ Radix/المكوّنات الشائعة | `dist/assets/` |
| M4 | **مكتبتا تواريخ** في المشروع معًا (`date-fns` + `moment`) | `package.json` |
| M5 | **9 حزم ثقيلة غير مستخدمة**: `three`, `react-leaflet`, `react-quill`, `jspdf`, `html2canvas`, `canvas-confetti`, `@hello-pangea/dnd`, `lodash`, `framer-motion` — حجم تثبيت وسطح ثغرات بلا مقابل | `package.json` |
| M6 | **صور شعار ضخمة**: `icon-512.png` 368 KB، `icon-192.png` 60 KB، `apple-touch-icon.png` 56 KB — بلا WebP/AVIF | `public/` |
| M7 | **تعارض في سياسة الأذونات**: الترويسة تنفي `geolocation`، بينما إطار HELM Smart يطلب `allow="geolocation"` — الطلب لن ينجح أبدًا | `vercel.json` ↔ `src/pages/HelmSmart.jsx:459` |
| M8 | **معرّفات النطاق مثبّتة في `index.html`** (`https://helm-protal.vercel.app`) مع وجود نشر Cloudflare (`wrangler.jsonc`) — أي تغيير نطاق يكسر canonical و hreflang و og:url | `index.html` |
| M9 | **`npm run typecheck` يفشل** دائمًا (أخطاء `checkJs` سابقة) — سكربت مضلّل؛ إمّا إصلاحه أو إزالته من الواجهة | `jsconfig.json` |
| M10 | **ملف واحد 1 سطر** `src/lib/PageNotFound.jsx` يقف بجانب `src/PageNotFound.jsx` (38 سطرًا) الميت — تشويش بنيوي | `src/` |

### 🟢 منخفضة

- 18 تحذير ESLint متبقٍ (متغيّرات غير مستخدمة) — لا تفشل البوابة لكنها ضجيج.
- 10 ملفات توثيق عربية في جذر المشروع (`README_*_AR.md` ×6، `MIGRATION_STATUS_AR.md`…) — يُفضّل نقلها إلى `docs/`.
- ملفات ميتة مؤكدة يمكن حذفها بأمان: `src/pages/Dashboard.jsx` (476 سطرًا، بديله `DashboardOptimized`)، `src/PageNotFound.jsx`، `src/components/UserNotRegisteredError.jsx`، `src/components/app/SidebarMiniStats.jsx`، `src/components/shared/DeleteConfirmDialog.jsx`، `src/components/shared/PageSkeleton.jsx`.
- سكربتات `dev`/`build` تستدعي `node node_modules/vite/bin/vite.js` بدل `vite` — غير معتاد لكنه يعمل.

---

## 5. خطة التطوير المقترحة

### المرحلة 0 — الأساس (✅ أُنجزت في هذه المراجعة)
فحص نظيف · 81 اختبارًا · بوابة CI · رؤوس أمان · إصلاح XSS · إصلاح رابط الدفع · حذف ملفات المفاتيح الميتة.

### المرحلة 1 — إغلاق ثغرات الصلاحيات (أسبوع · أولوية قصوى)

1. **نقل قرارات الصلاحية إلى قاعدة البيانات.** تحويل «مدير التشغيل» و«منع الحذف» إلى عمود `role` في `user_profiles` وإلى سياسات RLS. حذف البريد المُثبَّت من `src/App.jsx` و `ActionButtons.jsx`.
2. **تدوير أي مفتاح مرّ من `src/lib/*test.js`** ومراجعة أسرار Gemini/Stripe/Supabase.
3. **إزالة `VITE_GEMINI_API_KEY` من `.env.example`** ونقل أي ذكاء اصطناعي إلى Supabase Edge Function — المفتاح يبقى في الخادم (`supabase/functions/` جاهزة لهذا النمط).
4. **تحويل CSP من «تقرير فقط» إلى إلزامي** بعد جمع الإنذارات أسبوعًا، مع نقل السكربت المضمّن في `index.html` (تهيئة الثيم) إلى ملف خارجي ليصبح `script-src 'self'` كافيًا.
5. **توحيد الهجرات**: إعادة ترقيم `003`/`010`/`022` المكررة، ونقل ملفات SQL من الجذر إلى `supabase/migrations/` كملفات مرقّمة، وتوثيق ترتيب التنفيذ الواحد الصحيح في `docs/SQL_RUN_ORDER_AR.md`.
6. **تشديد RLS لفصل نطاق الموكّل** والتحقق من أن `client_id` لا يمكن تزويره من الواجهة.

### المرحلة 2 — الأداء (أسبوعان)

1. **إخراج `recharts` من المسار الحرج**: مخطط الشرارة في اللوحة الرئيسية يُستبدل بـ SVG مضمّن (~30 سطرًا) → **توفير ~114 KB مضغوط من أول تحميل**. تبقى `recharts` لصفحة التقارير عبر تحميل كسول.
2. **تحميل الخط المختار فقط**: نقل الخطوط من `@import` إلى تحميل ديناميكي حسب `settings.app_font` → توفير كبير في أول رسم.
3. **حذف الحزم التسع غير المستخدمة** (`three`, `react-leaflet`, `react-quill`, `jspdf`, `html2canvas`, `canvas-confetti`, `@hello-pangea/dnd`, `lodash`, `framer-motion`) وتوحيد `moment` على `date-fns`.
4. **ضغط الشعارات** إلى WebP/AVIF مع `srcset` (من 368 KB إلى ما دون 30 KB).
5. إضافة `manualChunks` لمكوّنات Radix الشائعة وتفعيل تحليل الحزم.

### المرحلة 3 — المعمارية (شهر)

1. **تفكيك `base44Client.js`** إلى وحدات: `auth`، `entities`، `scopes`، `storage`، `cache`. وإعادة تنسيق الأسطر المضغوطة لتصبح قابلة للتدقيق — هذه الطبقة هي حدود الأمان ويجب أن تُقرأ في دقائق لا في ساعات.
2. **تحديد مصير `src/smart/`**: إمّا حذفه (تطبيق HELM Smart منشور منفصلًا على Cloudflare) أو نقله إلى مستودع خاص به. 10,981 سطرًا ميتة تكلفة مستمرة في كل تدقيق.
3. **حذف الملفات الميتة** المذكورة في القسم 4 🟢 وتوحيد `PageNotFound` و `Dashboard` و `PublicEntry`.
4. **توسيع الاختبارات** إلى مسارات المستخدم الحرجة: تسجيل الموكّل، إصدار فاتورة، تسجيل دفعة، تحويل قضية إلى مكتملة (Vitest + Testing Library + محاكاة Supabase).
5. **إصلاح `typecheck`** أو إزالته، ورفع تحذيرات ESLint المتبقية إلى الصفر، وتشغيل `--max-warnings 0`.

### المرحلة 4 — التطوير الوظيفي (مقترحات عمل)

- **لوحة تحصيل مالية**: أعمار الديون، متابعة آلية للفواتير المتأخرة عبر واتساب (البنية موجودة في `whatsappTemplates.js`).
- **ربط بوابة الموكّل بـ HELM Smart** عبر `postMessage` بعقد أنواع موثّق ومُتحقَّق من المصدر.
- **تقرير امتثال شهري** يُصدَّر PDF من نشاط المستخدمين الموجود في `UserActivity`.
- **مسح ضوئي بالمحمول** لرفع المستندات مباشرة إلى القضية (رفع الملفات موجود؛ ينقصه مسار الكاميرا).

---

## 6. قرارات مطلوبة منك

1. **هل أُنفّذ المرحلة 1 كاملة الآن؟** (نقل الصلاحيات إلى RLS وحذف البريد المُثبَّت — يمسّ سلوك «مدير التشغيل» ويحتاج تأكيدك على من يملك أي صلاحية).
2. **هل أستبدل مخطط `recharts` في اللوحة الرئيسية بـ SVG؟** (توفير 114 KB من أول تحميل، مع احتمال تغيّر بصري بسيط جدًا في شكل المخطط).
3. **ما مصير `src/smart/`؟** حذف / نقل إلى مستودع منفصل / إبقاء.
4. **هل أحذف الملفات الميتة المؤكدة** (7 ملفات + `Dashboard.jsx`)؟
5. **ما النطاق النهائي للمنصة؟** `helm-protal.vercel.app` أم نطاق مخصّص — يؤثر على `index.html` و canonical.
6. **هل لديك مفتاح Gemini أو غيره يعمل داخل بوابة HELM؟** إن نعم، فأين يُقرأ، وكيف ننقله إلى Edge Function.

---

## 7. أوامر التحقق السريعة

```bash
npm ci              # تثبيت نظيف
npm run lint        # يجب أن يمر بلا أخطاء
npm test            # 81 اختبارًا
npm run build       # بناء الإنتاج
npm run verify      # الثلاثة معًا — بوابة ما قبل النشر
```

**قبل النشر:** اختبر يدويًا (1) توليد وطباعة وثيقة من قالب، (2) فتح رابط دفع لفاتورة بمعرّف نصي غير UUID، (3) تسجيل دخول موكّل وتأكيد أنه لا يرى إلا صفحاته المسموحة.
